apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "resume-review.fullname" . }}-test-connection"
  labels:
    {{- include "resume-review.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "resume-review.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
