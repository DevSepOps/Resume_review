apiVersion: v1
kind: Service
metadata:
  name: {{ include "resume-review.fullname" . }}
  labels:
    {{- include "resume-review.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "resume-review.selectorLabels" . | nindent 4 }}
